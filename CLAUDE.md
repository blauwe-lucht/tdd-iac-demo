# Conventions

- In `tdd-python/`, use only single blank lines as separators (between imports, functions, and statement groups) — not PEP 8's double blank line before top-level definitions. This keeps more code visible on screen during the demo.
